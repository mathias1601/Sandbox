/* import React from 'react'
import DropdownButton from '../DropdownButton/DropdownButton'
import DropdownContent from '../DropdownContent/DropdownContent'

interface Props {
    buttonText: string,
    content: string
}

function Dropdown( {buttonText, content}: Props ) {
  return (
    <div>
        <DropdownButton>
            {buttonText}
        </DropdownButton>
        <DropdownContent>
            {content}
        </DropdownContent>
    </div>
  )
}

export default Dropdown */