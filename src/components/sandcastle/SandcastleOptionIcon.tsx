import OptionIcon, { type SandcastleOptionIconProps } from './svg/OptionIcon';

function SandcastleOptionIcon(props: SandcastleOptionIconProps) {
  return <OptionIcon {...props} />;
}

export default SandcastleOptionIcon;
